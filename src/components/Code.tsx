
import { LucideIcon } from 'lucide-react';
import { Code as CodeIcon } from 'lucide-react';

// Use the existing CodeIcon component instead of creating a custom one
const Code: LucideIcon = CodeIcon;

export default Code;
